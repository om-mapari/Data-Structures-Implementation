










int main()
{
    int a[] = {6, 2, 3, 1, 9, 10, 15, 13, 12, 17}; // creating an array of integers.
    int n;
    n = sizeof(a) / sizeof(a[0]);
    // MergeSort(a, n);

    // for (int atom : a)
    // {
    //     cout << atom << " ";
    // }
    return 0;


    
}